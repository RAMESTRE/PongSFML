#ifndef RENDER_WINDOW
#define RENDER_WINDOW

class RenderWindow {
public:

	RenderWindow();
	virtual ~RenderWindow();

	void displayWindow() const;

private:


};

#endif // !RENDER_WINDOW
